@page "/assets"
@using AssetManagement.Shared.Models 
@using AssetManagementApp.Services 
@inject IAssetClientService AssetService
@inject Microsoft.JSInterop.IJSRuntime JSRuntime

<h3>Asset Inventory</h3>

@if (assets == null)
{
    <p><em>Loading assets...</em></p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Name</th>
                <th>Serial Number</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var asset in assets)
            {
                <tr>
                    <td>@asset.Name</td>
                    <td>@asset.SerialNumber</td>
                    <td>@asset.Status</td>
                    <td>
                        <a href="/assets/edit/@asset.Id" class="btn btn-sm btn-primary">Edit</a>
                        <button @onclick="() => DeleteAsset(asset.Id)" class="btn btn-sm btn-danger">Delete</button>
                    </td>
                </tr>
            }
        </tbody>
    </table>
    <a href="/assets/create" class="btn btn-success">Add New Asset</a>
}

@code {
    private List<Asset>? assets;

    protected override async Task OnInitializedAsync()
    {
        await LoadAssets();
    }

    private async Task LoadAssets()
    {
        try
        {
            // FIX: Use the GetAllAssetsAsync method name to match the service interface
            assets = await AssetService.GetAllAssetsAsync();
        }
        catch (Exception ex)
        { 
            // In a real app, you'd show this error to the user
            Console.WriteLine($"Error fetching assets: {ex.Message}");
            // Initialize to an empty list to show the "Add New Asset" button
            assets = new List<Asset>(); 
        }
    }

    private async Task DeleteAsset(int id)
    {
        // NOTE: The JSRuntime invoke syntax has been corrected here for best practice.
        if (await JSRuntime.InvokeAsync<bool>("confirm", "Are you sure you want to delete this asset?"))
        {
            await AssetService.DeleteAssetAsync(id);
            await LoadAssets();
        }
    }
}